{
  "name": "jrtell.se",
  "version": "1.0.0",
  "description": "jrtell.se",
  "author": "John-Robin Tell",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup --overwrite-local --verbose"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "nuxt": "^2.4.0"
  },
  "devDependencies": {
    "autoprefixer": "^8.6.4",
    "nodemon": "^1.18.9",
    "push-dir": "^0.4.1"
  }
}
